version: "1"
packages:
  - name : "db"
    path : "./db/db"
    schema : "./db/migrations/"
    queries : "./db/query/"
    engine : "postgresql"
    emit_prepared_queries: false
    emit_json_tags : true
    
